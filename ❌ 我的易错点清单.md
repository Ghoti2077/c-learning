---
title: "❌ 我的易错点清单"
type: errorlist
---
# ❌ 我的易错点清单

> 每次 AI 批改我的笔记，错的地方都汇总到这张表。复习时先看这里，比看笔记管用。
> 状态：⬜ 还没改过来 / 🟢 已经不会再犯

| 日期         | 易错点                                                                                   | 正确写法                                                                                                                                  | 来自哪课                               | 状态       |
| ---------- | ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- | -------- |
| 2026-09-13 | `return0;` 挤在一起写                                                                      | `return 0;`（有空格 + 英文分号）                                                                                                               | [[L01 main函数与程序框架]]                | ⬜        |
| 2026-09-13 | 把 `main` 当成了"返回类型"                                                                    | `int` 是返回值类型，`main` 是**函数名**                                                                                                          | [[L01 main函数与程序框架]]                | 🟢 复习题答对 |
| 2026-09-13 | 以为"每行都要加分号"                                                                           | `#include`、`int main()`、`{`、`}` 后面**不加**分号                                                                                            | [[L01 main函数与程序框架]]                | ⬜        |
| 2026-09-13 | 以为代码全都要写在 `{}` 里                                                                      | `#include` 在 `{}` 外面；只有可执行语句在函数体 `{}` 里                                                                                               | [[L01 main函数与程序框架]]                | ⬜        |
| 2026-09-13 | 把 F11 当成"运行程序"                                                                        | F11 = 逐语句调试（会进函数）；跑程序用 `Ctrl+F5`                                                                                                      | [[Visual Studio 快捷键与调试]]           | ⬜        |
| 2026-09-13 | 分不清哪行代码是对的（把正确的 `return 0;` 也判成编译不过）                                                  | 判断代码对错别靠感觉，**让编译器跑一遍**看报错                                                                                                             | [[L01 main函数与程序框架]]                | ⬜        |
| 2026-09-13 | 不知道"没有 main 会怎样"                                                                      | 编译能过，**链接**时挂（`LNK2019 无法解析的外部符号 _main`）→ 系统找不到入口                                                                                     | [[L01 main函数与程序框架]]                | ⬜        |
| 2026-09-13 | 头文件拼错：`stadio.h`                                                                      | 正确是 **`stdio.h`**（s-t-d-i-o，**没有 a**），且**必须带 `.h`**                                                                                   | [[L02 库函数与头文件（printf、stdio.h、命名）]] | ⬜        |
| 2026-09-13 | `stander` 拼错                                                                          | 是 **standard**（标准）。stdio = standard input/output                                                                                      | [[L02 库函数与头文件（printf、stdio.h、命名）]] | ⬜        |
| 2026-09-13 | 占位符写成 `#d`                                                                            | 占位符是 **`%d`**（百分号）；`#` 是预处理符号（`#include`/`#define` 才有）                                                                                | [[L02 库函数与头文件（printf、stdio.h、命名）]] | ⬜        |
| 2026-09-13 | 以为 `int ban;` 是"创建函数名"                                                                | `int ban;` = **变量**（盒子）；`int ban();` 才是**函数**。看有没有 `( )`                                                                              | [[L02 库函数与头文件（printf、stdio.h、命名）]] | ⬜        |
| 2026-09-13 | 以为"库函数放在头文件里"（以为实现也在里面）                                                               | 头文件放**声明**（说明书），**实现**在库文件里                                                                                                           | [[L02 库函数与头文件（printf、stdio.h、命名）]] | ⬜        |
| 2026-09-13 | 把 `#include` 理解成"抽象地列举清单"                                                             | 它是**把整页文字原样粘贴进来**（预处理阶段，实测 7 行 → 1805 行）                                                                                              | [[L02 库函数与头文件（printf、stdio.h、命名）]] | ⬜        |
| 2026-09-13 | 答题只答一半（问"为什么错"只答了"它是什么"）                                                              | 看到问"为什么"就**必须给原因**，别只答现象                                                                                                              | [[L02 库函数与头文件（printf、stdio.h、命名）]] | ⬜        |
| 2026-09-13 | `ASCII` 拼成 `ASXII`                                                                    | 是 **ASCII**（A-S-C-I-I），American Standard Code for Information Interchange                                                             | [[L03 字符与ASCII码]]                  | ⬜        |
| 2026-09-13 | 以为"字符 = 键盘上能敲出来的"（可自己写的 `\n` 就敲不出来）                                                   | 敲不出来的也是字符（`\n` `\t` `\a`），它们是**控制字符**；字符 = 单个符号                                                                                       | [[L03 字符与ASCII码]]                  | ⬜        |
| 2026-09-13 | 把 0~31 笼统说成"不可打印的字符"                                                                  | 精确叫**控制字符**；**127（DEL）也不可打印**；**32（空格）可以打印**                                                                                          | [[L03 字符与ASCII码]]                  | ⬜        |
| 2026-09-13 | 把 `' '` 当成"中间没有字符"（判它非法）                                                              | `' '` 中间是**一个空格字符**，合法，值 **32**；真一无所有的是 `''`，报 `empty character constant`                                                             | [[L03 字符与ASCII码]]                  | ⬜        |
| 2026-09-15 | 把乱码归因于"创建数组时没用双引号"                                                                    | 真正原因是**数组里没有 `\0`**；`{'a','b','c','\0'}` 没用双引号也能正常打印。双引号只是**自动帮你补 `\0`**                                                              | [[L04 字符串与结束标志]]                   | ⬜        |
| 2026-09-15 | 把 0xCC 的因果搞反："程序把垃圾内存填成 0xCC"                                                         | 顺序是 **VS 预先填 0xCC → 程序读到 → 控制台按 GBK 显示成"烫"**。程序只负责**读**，从没往内存写过东西                                                                     | [[L04 字符串与结束标志]]                   | ⬜        |
| 2026-09-15 | 表述"电脑不知道后面是什么内容"                                                                      | 精确说：电脑**看得到也会读**后面的内容，问题是不知**该在哪儿停**（`printf("%s")` 靠 `\0` 才知道停）                                                                      | [[L05 转义字符]]                       | ⬜        |
| 2026-09-15 | 举例说 `'x'` 和 `'\130'` 输出相同（实际不相等）                                                      | `'x'` = **120**（小写 x）；`'\130'` = 八进制 130 = **88** = **大写 `X`**。举等值例子要用 `'Q'` = `'\x51'` = `'\121'` = `81`。**教训：八进制/十六进制转义先换算成十进制再比较** | [[L05 转义字符]]                       | ⬜        |
| 2026-09-15 | `sizeof` 打成 `zizeof`                                                                  | 是 **sizeof**（s 开头），写错在 VS 里就是"未声明的标识符"                                                                                                | [[L06 字符串长度 strlen]]               | ⬜        |
| 2026-09-15 | `\0` 打成 `/0`（反斜杠写成斜杠） | `\`（反斜杠）= 转义符；`/`（斜杠）= 除法/注释符号。`"/0"` 只是普通斜杠+数字，跟空字符无关 | [[L06 字符串长度 strlen]] | 🟢 默写答对（用了反斜杠） |
| 2026-09-15 | 🔁 **第 2 次**：`\n` 打成 `/n`                                                             | `printf("i like C/n")` 会**原样打出 `i like C/n`**（不报错，只是结果不对）。**盯住方向：反斜杠往左倒 `\`，斜杠往右倒 `/`**                                               | [[L09 数据类型与布尔类型]]                  | ⬜        |
| 2026-09-15 | **多个 `#include` 挤在同一行**（`#include <stdio.h>  #include <string.h>#include<stdbool.h>`） | 预处理指令**必须一行一条**。挤一起 → 只有第一条生效，后面全被丢弃（VS 报 `C4067 预处理器指令后有意外标记`）→ `true` 才报未定义                                                         | [[L09 数据类型与布尔类型]]                  | ⬜        |
| 2026-09-15 | 布尔真假**记反**了（写成"0 表示真、非零表示假"）                                                          | 正好相反：**0 = 假，非零 = 真**（-1 也是真）。这条在以后判断寄存器位时天天用                                                                                         | [[L09 数据类型与布尔类型]]                  | ⬜        |
| 2026-09-15 | 以为 `_Bool` 也需要 `<stdbool.h>`                                                          | `_Bool` 是**关键字**，不需要任何头文件；需要 `<stdbool.h>` 的是 **`bool` / `true` / `false`** 这三个化名                                                     | [[L09 数据类型与布尔类型]]                  | ⬜        |
| 2026-09-15 | **字符串写成两行**（`"` 和 `"` 不在同一行）                                                          | 报 `missing terminating " character`。要换行就打 `\n`，不能直接回车                                                                                 | [[L08 注释（不能嵌套的原因）]]                | ⬜        |
| 2026-09-15 | 答"四族内置类型"时**漏了字符型 char**，把 `void` 当成了数据族 | 四族 = **char / 整型 / 浮点型 / `_Bool`**；`void` 是**空类型**（表示"没有"），**不装数据**、不算五族 | [[L09 数据类型与布尔类型]] | ⬜ |
| 2026-09-16 | `sizeof(n = n + 100)`（n 是 int）答成 2，应该是 **4** | **`sizeof` 看的是"表达式的结果类型"**：`s` 是 short → 2；`n` 是 int → 4。别把上一题的数字搬过来 | [[L10 sizeof（关键字、操作符、字节与比特）]] | ⬜ |
| 2026-09-17 | 连续自增的题算错：`a=5; printf(a++); printf(++a); printf(a);` 先答 566，应为 **577** | 🟢 **当轮自己改对了**。要点：第一行 `a++` 已把 a 从 5 变成 6，第二行必须从 **6** 出发 → 7。**算连续题时，每行结束先写下"现在 a 是几"** | [[L12 复合赋值、递增递减、前缀与后缀]] | 🟢 当轮改正 |
| 2026-09-20 | 默写时**想不起 `sizeof`**（答"忘记了"） | `sizeof` —— 算"占几字节"的关键字/操作符，是 L10 的主角。**拼写 s-i-z-e-o-f**；在 VS 里写错 = 未声明的标识符 | [[L10 sizeof（关键字、操作符、字节与比特）]] | ⬜ |
| 2026-09-20 | 换行转义符默写成 `\d` | 换行是 **`\n`**（n = newline）；`\d` 不存在。容易和 `\0`（空字符）混——**`\0` 是数字 0，`\n` 是字母 n** | [[L05 转义字符]] | ⬜ |
| 2026-09-20 | `if (x = 0)` 之后**忘了 x 已经被改成 0**（答成 x = 3） | 赋值**真的会发生**：`x = 0` 让 x 变成 0、表达式值为 0 → 假 → 走 else。**跑完 x = 0**（实测）。上次 `if (x = 5)` 是相反情况：x 变 5、进 if | [[L15 if语句与关系运算符]] | ⬜ |
| 2026-09-21 | ❌ **`main` 打成 `mian`**（求最大值程序），VS 报 `LNK2019 无法解析的外部符号 main` + `LNK1120` | 是 **`main`**（m-a-i-n）。**编译能过、链接才挂**——因为 `mian` 语法合法、只是"入口名字"不对。看到 `LNK` 开头先想：拼错名字 / 漏函数 / 两个 main | [[L17 if else 与多分支（else 配对、常见坑）]] | ⬜ |
| 2026-09-21 | 找零程序写成 `if (bill > price)`，**漏掉"刚好相等"**（实测 80/80 → 输出"您的钱不够"） | 相等该算"找零 0 元"。用 **`>=`**，或补 `else if (bill == price)` → [[L17 if else 与多分支（else 配对、常见坑）]] 第 8 节 | [[L17 if else 与多分支（else 配对、常见坑）]] | ⬜ |
| 2026-09-21 | 提示语 `printf("请输入金额");` **结尾没有 `\n`** | 输入的数字会**接在同一行**（截图里"请输入金额93"）。提示语结尾加 `\n` | [[L17 if else 与多分支（else 配对、常见坑）]] | ⬜ |
| 2026-09-21 | ❌ **`printf("你输入的成绩是：", score);` 格式串里少了 `%d`** → 分数根本没打印出来（调试窗口里"成绩是："后面空空） | 要打印的值**必须在格式串里有对应占位符**：`printf("你输入的成绩是：%d\n", score);`。gcc 警告 `too many arguments for format`；**编译不报错、值就是不显示**，最容易漏看 | [[L18 缩进与花括号（if 管一条语句）]] | 🟢 复习题答对 |

## 反复犯的错（优先盯）
<!-- 同一个错犯第 2 次就往这里搬，红色警报 -->
🔥 **英文术语/函数名/符号凭记忆写 → 反复拼错（已 8 次）**
`stadio` ❌→`stdio`｜`stander` ❌→`standard`｜`ASXII` ❌→`ASCII`｜`zizeof` ❌→`sizeof`｜`/0` ❌→`\0`｜`/n` ❌→`\n`（**第 2 次**）｜**`mian` ❌→`main`（2026-09-21，害得链接直接失败）**
**反斜杠 `\` 和斜杠 `/` 要单独盯：`\` 往左倒、`/` 往右倒。写反了 VS 通常不报错，只是结果不对（更坑）。**
**对策：写英文术语和符号时对着课件/头文件抄，一个字母一个字母对。**
