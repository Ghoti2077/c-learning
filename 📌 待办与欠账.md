---
title: "📌 待办与欠账"
type: todo
---
# 📌 待办与欠账

> 这里放**没做完的事**：欠着的作业、没解决的问题、想回头验证的猜测。
> 做完了就划掉（`- [ ]` → `- [x]`），或者直接删掉那一行。

---

## 📝 欠着的作业

_（暂时没有欠账，清爽 ✨）_

---

## ❓ 还挂着的问题

### 等学到某个知识点再讲
- [ ] **宏（`#define`）** ← 2026-09-17 我明确说"先不用解释了，等我学到了再说"。**别催、别再讲**，等学到「预处理」那一章再打开 [[L14 什么是宏（define 入门）]]。（现在只要照抄那行修 `scanf` 的宏就行，不用懂）
- [ ] `unsigned` 里塞 `-1` 为什么变成 4294967295？→ **补码/模运算**，学到位运算（阶段9）讲透
- [ ] `short` 装 32768 会怎样？→ **溢出**，学到运算符时讲
- [ ] 三个 `#include` 挤一行那条为什么必须独占一行？→ 学「预处理」时讲
- [ ] `%zu` 到底是什么格式符？为什么要用它打印 `sizeof`？→ 学 printf 格式符时确认
- [ ] `#include <stdbool.h>` 里到底写了什么？→ 学「头文件/预处理」再看
- [ ] `[ ]` 数组的方括号到底啥意思？→ 等学数组

### 等上单片机时亲手验证（嵌入式专属）
- [ ] **ARM 上 `char` 默认是不是无符号？**（我在 PC 上实测：默认有符号、加 `-funsigned-char` 变无符号 → 说明是编译器说了算。ARM GCC 据说默认无符号，上板子验证一下）
- [ ] 单片机 RAM 很小时，选 `char` 还是 `int` 能省多少？（做个小实验）
- [ ] `sizeof(void)` 在 VS 里是报错还是给数字？

### 暂时没答案的（不用急着解决）
- [ ] 为什么 `long double` 我的 VS 是 8 字节、gcc 是 16 字节？（先记"平台相关"）

---

## ✅ 已还清

- [x] **2026-09-16 L09 作业：在自己 VS 里量各类型占几字节** ✅
  - 我的 VS 实测：`_Bool`=1、`char`=1、`short`=2、`int`=4、`long`=4、`long long`=8、`float`=4、`double`=8、**`long double`=8**
  - 对比 gcc：只有 `long double` 不一样（gcc 是 **16**）→ **平台相关，以自己机器为准**
  - 相关笔记：[[L10 sizeof（关键字、操作符、字节与比特）]]
- [x] 2026-09-15 「`' '` 到底是啥」→ 空格也是字符，值 32
- [x] 2026-09-15 「乱码为什么出现」→ 缺 `\0`，VS 预填 0xCC 显示成"烫"
- [x] 2026-09-15 「注释不支持嵌套是什么意思」→ 第一个 `*/` 就结束，不数层数
- [x] 2026-09-15 「`true` 为什么未定义」→ 三个 `#include` 挤一行，后面的失效
- [x] 2026-09-16 「sizeof 是关键字还是操作符」→ 都是；不用 include、可省括号、编译期出结果
- [x] 2026-09-16 「字节和比特的区别」→ 1 字节 = 8 比特
- [x] 2026-09-16 「sizeof 里的表达式会不会执行」→ **不会**（`s = b+1` 没跑，s 还是 12）
- [x] 2026-09-16 「`float` 和 `long` 能不能加 signed/unsigned」→ **`long` 能，`float` 不能**（浮点自带正负号）
- [x] 2026-09-17 「输出乱码」→ 源码存 UTF-8 无BOM + 控制台 GBK = 乱码；**另存为「UTF-8 带签名」+ 重新生成** → **已验证中文正常** ✅ → [[中文输出乱码（源码编码问题）]]
- [x] 2026-09-17 「`scanf` 为什么在 VS 里报 C4996」→ VS 嫌它不安全；**文件第一行**加 `#define _CRT_SECURE_NO_WARNINGS 1`（必须写在所有 `#include` 之前）→ [[VS 常见报错速查]]
