# 🔄 会话交接（新会话开局读这一篇）

> 用途：我把你在 `c-learning` 库里的学习情况交接给**任何新会话**。新会话说一句
> **"读 `D:\APP\hermes\c-learning\_交接.md`，然后继续教我 C"** 就能无缝接上。
> 最后更新：2026-09-21（当前进行到 L17）

---

## 1. 我在学什么

- **C 语言 → 嵌入式方向**，教材：B站【鹏哥C语言2026完整版】**BV17a7K64ELH**
- 我的水平：**AI 小白 + C 零基础**，一路从 `main` 函数学起
- 教学要求（详见 skill `c-learning-plan`）：**当傻子教**（先打比方 → 给代码 → 逐行解释，不跳步）、**必须指出我笔记里的错误**、每课出题 + 讲评、**允许欠账不逼我**
- **超纲的东西别硬讲**：我说过"先不用解释了"的知识（目前：**宏 `#define`**）就封存，等学到再说

## 2. 我的工作方式（每次都是这个流程）

1. 我把**笔记**发给你（文字，或写在 **VS 代码注释里 + 截图**；截图常在
   `D:\APP\hermes\picture\` 或聊天附件）
2. 你**逐句核对**：✅ 对 / ⚠️ 半对 / ❌ 错，把错的单独讲清（"错在哪 + 正确是什么"）
3. 你**用编译器实测**验证（gcc 在 `D:\APP\msys64`，注意用干净环境；也可以用 VS2026 的 cl）
4. 归档成 Obsidian 笔记 `01-基础语法/Lxx ...md`（引用我原话 + 实测证据 + 复习题）
5. 出 2~6 道**复习题** → 我答 → 你讲评 → 错题进 `❌ 我的易错点清单.md`

> 我的截图有时在 `D:\APP\hermes\picture\`，有时是聊天附件；文件名可能带空格。
> **VS 环境**：VS2026 装在 `D:\VS2026`，我的工程在 `D:\VS2026\code\study1`，源码编码要存成 **UTF-8 带签名**（否则中文乱码）。

## 3. 现在进行到哪儿了

- 已完成：**L01 ~ L17**（入门 → 字符/字符串 → 数据类型 → sizeof → signed/unsigned → 运算符 → if → if/else 多分支）
- 最近一课：**L17 if…else 与多分支（else 配对、三个坑、找零计算器完整版）**（6 道复习题已发，**还没答**）
- ⏳ **还欠着 L16 的 6 道复习题**（运算符优先级那课，别催，等他答）
- 我的第 2 个实用程序：**找零计算器**（L17 已用 if/else 修好"票面不够"的负数问题）
- 易错点清单：**约 35 条**，其中红色警报是 **英文术语/符号反复拼错**（`stdio`、`sizeof`、`ASCII`、反斜杠方向）
- 每日 8 点有自动复习（cron 任务"每日复习"从易错点清单出题）

## 4. 库的结构（都在 `D:\APP\hermes\c-learning`）

| 位置 | 是什么 |
|---|---|
| `🏠 C学习首页.md` | **自动生成的进度索引**（先看这个） |
| `01-基础语法/` | 课程笔记 L01 ~ L16 |
| `04-工具与调试/` | VS 快捷键、**VS 常见报错速查**、中文乱码 |
| `❌ 我的易错点清单.md` | 所有错过的点（⬜ 未改 / 🟢 已掌握） |
| `📌 待办与欠账.md` | 欠着的作业 + 还没搞懂的问题 |
| `_附件/` | 全部原始截图（课件 + 我的 VS 实测） |
| `_tools/check_vault.py`、`make_index.py` | 每 5 次对话自动**体检 + 刷新首页** |

## 5. 约定（别忘）

- 笔记里**用 callout 折叠**（`> [!question]- 标题`），**不要用 HTML `<details>`**（Obsidian 不渲染）
- 文件名**不能带 `#`**（会被当成小标题锚点）
- **每 5 次对话**顺手跑：`cd "D:/APP/hermes/c-learning/_tools" && python check_vault.py --tick`
- 回复**尽量省 token**：细节进笔记，聊天只给结论
- 复习题答完要**更新易错点清单状态**（答对 → 🟢）

## 6. 完整对话档案（可选读取）

本次会话（ID `20260913_201009_67019b`）的完整记录导出在：
`D:\APP\hermes\_会话档案\` —— 需要原话细节时去翻，**平时不用读**（太大）。

也可以在任意会话里直接引用：`@session:default/20260913_201009_67019b`
