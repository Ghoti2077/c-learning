# 🔄 会话交接（新会话开局读这一篇）

> 用途：我把你在 `c-learning` 库里的学习情况交接给**任何新会话**。新会话说一句
> **"读 `D:\APP\hermes\c-learning\_交接.md`，然后继续教我 C"** 就能无缝接上。
> 最后更新：2026-09-21（当前进行到 L17）

---

## 1. 我在学什么

- **C 语言 → 嵌入式方向**，教材：B站【鹏哥C语言2026完整版】**BV17a7K64ELH**
- 我的水平：**AI 小白 + C 零基础**，一路从 `main` 函数学起
- 教学要求（详见 skill `c-learning-plan`）：**当傻子教**（先打比方 → 给代码 → 逐行解释，不跳步）、**必须指出我笔记里的错误**、每课出题 + 讲评、**允许欠账不逼我**
- **超纲的东西别硬讲**：我说过"先不用解释了"的知识（目前：**宏 `#define`**）就封存，等学到再说

## 2. 我的工作方式（每次都是这个流程）

1. 我把**笔记**发给你（文字，或写在 **VS 代码注释里 + 截图**；截图常在
   `D:\APP\hermes\picture\` 或聊天附件）
2. 你**逐句核对**：✅ 对 / ⚠️ 半对 / ❌ 错，把错的单独讲清（"错在哪 + 正确是什么"）
3. 你**用编译器实测**验证（gcc 在 `D:\APP\msys64`，注意用干净环境；也可以用 VS2026 的 cl）
4. 归档成 Obsidian 笔记 `01-基础语法/Lxx ...md`（引用我原话 + 实测证据 + 复习题）
5. 出 2~6 道**复习题** → 我答 → 你讲评 → 错题进 `❌ 我的易错点清单.md`

> 我的截图有时在 `D:\APP\hermes\picture\`，有时是聊天附件；文件名可能带空格。
> **VS 环境**：VS2026 装在 `D:\VS2026`，我的工程在 `D:\VS2026\code\study1`，源码编码要存成 **UTF-8 带签名**（否则中文乱码）。

## 3. 现在进行到哪儿了

- 已完成：**L01 ~ L23**（入门 → …… → switch-case → PAT 刷题 → **while 循环第一课**）
- 最近一课：**L23 `while` 循环第一课（数位数）+ `else if` 条件顺序** —— 他自己总结的规律"**`>=` 时从大到小、`<` 时从小到大**" **✅ 完全正确**（AI 穷举 0~120：两种正确排法各错 0 处；`<` 配大到小错 **80 处**）；他问"注释掉课件里 `n++; x /= 10;` 那两行为什么结果没变"→ 实测**只有输入 0（或负数）时才不同**（有那两行 0→1、注释掉 0→0），那两行是"**先手动算掉一位**"，专门兜住"进不去循环"的输入。**顺便立了一条测试纪律：判断某行有没有用必须试边界（这里就是 0）**
- 上一课：**L22 PAT「分支-06 成绩转换」** —— 老师点了一句"用分数的十位数判断"，他就写出了 `grade /= 10; switch(grade)`，并且**用 `case 10: case 9:` 穿透**让 100 分和 90+ 共用一句 `printf("A\n")` ✅ 思路漂亮。**两个必须改的**：① `<60` 应输出 **E**，他写成 **F**（AI 穷举 0~120 → **71 处不一致，全是这个**）；② 自测用的 `printf("输入成绩:");` 没删 → **交 OJ 直接 WA**（实测输出 `输入成绩:A` vs 要求 `A`）。他这版 printf **都带 `\n` 了**（红警见效）
- 上一课：**L21 `switch-case`（`default` 是什么、`break` 到底中断谁、类型/常量限制）** —— 批改了他的四句话：`break` 是"跳出整个 switch"不是"中断 case" ✅ 已实测穿透现象；`default` 已讲透（等价于最后的 `else`，可省略、可放中间）；控制表达式必须是**整型**（char 可以、float 不行）；`case` 只能放**编译期整型常量**——**连 `const int` 变量都不行**（gcc/VS C2051 双实证）；他的 switch 版程序逻辑全对、`break` 一个没漏，但**`printf` 漏 `\n` 第 3 次 → 已升红警**
- 上一课：**L20 `else if` 级联是什么 + 单一出口 vs 多出口** —— 他的疑问"`else if` 是不是理解成'否则就执行'"：`else` 才是"否则就执行"，`else if` 是"**否则，再检查一个新条件**"（= `else` 里套 `if`，已用展开写法 + 探针实验证明条件真会被检查）；他判断"左边是单一出口" ✅ 正确；**已实操（问候语级联程序，`==` 全对）+ 复习题讲评已归档**；L19 欠的第 2 题也补答了 ✅
- ⚠️ **我自己的失误记录**：L20 参考答案原先写"换级联后检查次数会减少"——**实测数据推翻了它**（`x=7` 命中最后一条时仍是 3 次），已在笔记里改正；教训：**结论必须用探针程序数过再写**
- 上一课：**L19 `const int` 是什么 + `else` 永远配最近的 `if`（缩进骗不了它）** —— 他的理解"else 与最近的 if 匹配，除非有大括号"✅ 正确；已归档 3 张附件；**三值最大值程序经 AI 穷举 343 组验证 0 错**；顺手抓到 `REAADY`（应 `READY`）拼错——编译不报错的那种，第 9 次拼错
- 上一课：**L18 缩进是什么 + if 的花括号什么时候能省**（他问的两个问题：①缩进啥意思 ②有 else 时前面的 printf 为啥不带 `{}`；已归档 3 张附件 + 实测证据；顺手抓到 `printf` 漏 `%d` 的真 bug）
- 上一课：**L17 if…else 与多分支（else 配对、三个坑、找零计算器完整版）** —— 已发 6 道复习题；2026-09-21 **他把实操（VS 截图 3 张）发来了，已逐句核对并归档**（第 8 节：找零 if/else 版、求最大值程序、`mian` 拼错 → LNK2019 案例、翁恺课件流程图对照）
- 📌 **他的 `mian` 拼错是个大事件**：`main` → `mian` 编译过、链接挂（`LNK2019` + `LNK1120`），已进易错点清单 + 「反复犯的错」（拼错累计 **9 次**）
- 🎬 **教材其实有两套**：鹏哥 BV17a7K64ELH（主线）+ **翁恺《C语言程序设计》（浙大，他也看，附了 else 那节流程图）** —— 讲同一知识点时可以互相对照
- ⏳ **欠账（2026-09-21 他本人说"先欠着"，别催）**：L16 复习题 6 道 + L17 复习题 3 道和手打作业（记在 `📌 待办与欠账.md`）
- 我的第 2 个实用程序：**找零计算器**（L17 已用 if/else 修好"票面不够"的负数问题）
- 易错点清单：**约 35 条**，其中红色警报是 **英文术语/符号反复拼错**（`stdio`、`sizeof`、`ASCII`、反斜杠方向）
- 每日 8 点有自动复习（cron 任务"每日复习"从易错点清单出题）

## 4. 库的结构（都在 `D:\APP\hermes\c-learning`）

| 位置 | 是什么 |
|---|---|
| `🏠 C学习首页.md` | **自动生成的进度索引**（先看这个） |
| `01-基础语法/` | 课程笔记 L01 ~ L16 |
| `04-工具与调试/` | VS 快捷键、**VS 常见报错速查**、中文乱码 |
| `❌ 我的易错点清单.md` | 所有错过的点（⬜ 未改 / 🟢 已掌握） |
| `📌 待办与欠账.md` | 欠着的作业 + 还没搞懂的问题 |
| `_附件/` | 全部原始截图（课件 + 我的 VS 实测） |
| `_tools/check_vault.py`、`make_index.py` | 每 5 次对话自动**体检 + 刷新首页** |

## 5. 约定（别忘）

- 笔记里**用 callout 折叠**（`> [!question]- 标题`），**不要用 HTML `<details>`**（Obsidian 不渲染）
- 文件名**不能带 `#`**（会被当成小标题锚点）
- **每 5 次对话**顺手跑：`cd "D:/APP/hermes/c-learning/_tools" && python check_vault.py --tick`
- 回复**尽量省 token**：细节进笔记，聊天只给结论
- 复习题答完要**更新易错点清单状态**（答对 → 🟢）

## 6. 完整对话档案（可选读取）

本次会话（ID `20260913_201009_67019b`）的完整记录导出在：
`D:\APP\hermes\_会话档案\` —— 需要原话细节时去翻，**平时不用读**（太大）。

也可以在任意会话里直接引用：`@session:default/20260913_201009_67019b`
